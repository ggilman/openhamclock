services:
  openhamclock:
    image: openhamclock:latest
    container_name: openhamclock
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - TZ=UTC
    volumes:
      - ./config:/config
